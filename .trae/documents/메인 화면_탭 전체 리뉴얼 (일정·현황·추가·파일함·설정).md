## 목표 UI/IA
- 바텀 탭을 5개로 재구성: 일정(캘린더), 현황(Pipeline), 추가(+), 파일함, 설정
- 전체 톤앤매너를 첨부 이미지처럼: 밝은 배경 + 라운드 카드 + 선명한 포인트 컬러 + 단순한 아이콘/여백

## 일정(캘린더) 리뉴얼
- [calendar_screen.dart](file:///c:/Deadline%20Note/lib/src/screens/calendar_screen.dart) 레이아웃을 1번 이미지 스타일로 재구성
  - 상단 헤더: “2026년 1월 ▾” 형태 + 우측 액션 아이콘(즐겨찾기/보기전환 등)
  - 상단 배너(요약/프로모션 카드) 추가(현재는 디자인용 카드로 구현)
  - 캘린더 셀에 일정이 있으면 ‘칩/필’ 형태로 1~N개 표시(길면 +N)
- 날짜 탭 시 2번 이미지처럼 “해당 날짜 일정 목록 화면(또는 바텀시트)”을 띄움
  - 상단에 날짜 텍스트, 우측에 추가(+) 버튼
  - 아래에 그 날짜에 저장된 회사/공고 리스트 카드
  - 하단에 광고 영역(우선 디자인용 배너/카드로 구현)

## 현황(Pipeline) 화면 신설(기존 리스트 대체)
- [list_screen.dart](file:///c:/Deadline%20Note/lib/src/screens/list_screen.dart)를 “현황” 화면으로 리뉴얼(새 파일 생성 최소화)
  - 상단 필터: 전체 / 지원완료 / 면접중 / 합격
  - 카드형 리스트: 상태별 섹션(서류/면접 단계 등) + 각 카드에 회사/직무/마감/상태 표시
  - 하단에 광고 영역(우선 디자인용 배너/카드)
- 상태 모델 확장
  - [job_status.dart](file:///c:/Deadline%20Note/lib/src/models/job_status.dart) 를 ‘서류, 화상면접, 1차, 2차, 최종, 합격/오퍼, 불합격’ 등으로 확장
  - [job_deadline.dart](file:///c:/Deadline%20Note/lib/src/models/job_deadline.dart) JSON 마이그레이션(기존 저장 데이터 호환) 반영

## 추가(+) 탭 동작 변경
- 바텀 탭 “추가” 선택 시 URL 입력 화면을 바로 표시
  - 기존 [add_from_share_screen.dart](file:///c:/Deadline%20Note/lib/src/screens/add_from_share_screen.dart) 재사용
  - 입력 → 파싱 → (미리보기 포함) 저장 시 캘린더에 반영

## 파일함(로컬 저장) + 메모 기능
- 파일함 화면 추가(필요 시 새 화면 파일 1개만 생성)
  - 상단 SegmentedControl로 “파일 / 메모” 전환(4번 이미지 느낌)
  - 파일: 자소서/포트폴리오/증명서 등 항목을 로컬(Internal Storage: app documents)에 복사 저장, 리스트로 관리
  - 메모: 간단 메모 CRUD, 로컬에만 저장
  - “앱 삭제 시 데이터가 삭제될 수 있음” 안내 카드 노출
- 필요한 패키지(현재 프로젝트에 없음 → 추가 예정)
  - path_provider(앱 문서 경로), file_picker(파일 선택), open_filex(파일 열기) 등

## 상세 화면(세부내역/편집/삭제/공유/알림)
- [deadline_detail_screen.dart](file:///c:/Deadline%20Note/lib/src/screens/deadline_detail_screen.dart) 를 3번 이미지처럼 개편
  - AppBar 우측 메뉴: 편집/복사/삭제/공유
  - 공유(타 앱 공유): share_plus 패키지 추가 후 텍스트/링크 공유
  - 알림 설정: ‘이 항목 알림 켜기/끄기’ 토글 + 현재 설정(D-3/D-1/3h) 기반 예정 알림 시간 표시
- 데이터/스케줄링 반영
  - JobDeadline에 per-item 알림 플래그 추가
  - [notifications_service.dart](file:///c:/Deadline%20Note/lib/src/services/notifications_service.dart), [app_state.dart](file:///c:/Deadline%20Note/lib/src/state/app_state.dart)에서 해당 플래그가 꺼진 항목은 스케줄 등록/유지하지 않도록 처리

## 하단 탭/네비게이션 구조 변경
- [home_shell.dart](file:///c:/Deadline%20Note/lib/src/screens/home_shell.dart) 를 3탭 → 5탭으로 확장
  - nested Navigator 구조 유지(탭별 back stack 유지)
  - “추가” 탭은 바로 입력 화면을 루트로

## 스타일/테마 정리
- [app_theme.dart](file:///c:/Deadline%20Note/lib/src/ui/app_theme.dart) 를 이미지 톤에 맞게 조정
  - 더 밝은 surface, 더 또렷한 포인트 컬러, 라운드/타이포 정리
  - NavigationBar, Card, ListTile, SegmentedButton 스타일 일괄 통일

## 광고 처리 방식(가정)
- 우선: 디자인용 “광고 배너 자리(placeholder)” 구현
- 실제 광고 SDK(google_mobile_ads) 연동은 별도 단계로 진행(스토어 설정/광고 ID 필요)

## 검증
- flutter analyze / 디버그·릴리즈 APK 빌드
- 수동 시나리오: 날짜 탭 → 목록 노출, 항목 탭 → 상세(편집/삭제/공유/알림), 현황 필터 동작, 추가 탭 저장, 파일함 저장/열기, 메모 저장

이 계획대로 바로 리뉴얼 작업을 진행할까요?